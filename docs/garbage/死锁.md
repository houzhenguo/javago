
## 死锁的代码部分

```java
public class DeadLockDemo {
    private static Object ob1 = new Object();
    private static Object bo2 = new Object();
    public static void main(String ... args) {
        new Thread(()->{
            synchronized(ob1){
                try{
                    Thread.sleep(2000);
                }catch(Exceptioin ex) {

                }
                synchronized(ob2){
                    ss
                }
            }
        }).start();


        // new Thread2 同理
    }
}
```

手写代码，代码核心部分是这样的。